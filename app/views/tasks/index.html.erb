<h2>Voici la liste des tâches</h2>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task)%><%= link_to "Delete",
  delete_task_path(task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%></li>
  <% end %>
</ul>
<%= link_to "😺Create new task", create_task_path(@task)%>
